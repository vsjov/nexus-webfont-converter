#![forbid(unsafe_code)]

//! Native CLI entry point. Argument parsing is added in implementation phase 10.

/// Starts the native CLI.
fn main() {
    eprintln!("The Rust wfc CLI is not available yet. Use the Node.js CLI during migration.");
    std::process::exit(1);
}
